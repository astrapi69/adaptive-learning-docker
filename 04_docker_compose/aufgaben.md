# Aufgaben: Multi-Container Setup

1. Erstelle eine `docker-compose.yml` mit zwei Services: `app` (Java) und `db` (Postgres).
2. Wie lautet der Befehl, um alle Services im Hintergrund zu starten?
3. Wie können sich die beiden Container im Netzwerk gegenseitig finden?
