# Lösungen: Multi-Container Setup

1. Siehe Beispiel im Protokoll (YAML Struktur).
2. `docker-compose up -d`
3. Über den Service-Namen (z.B. `ping db`). Docker Compose erstellt automatisch ein gemeinsames Bridge-Netzwerk.
