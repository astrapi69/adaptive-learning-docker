# Docker Compose

## Vorteile

- Definition mehrerer Container in einer `docker-compose.yml`.
- Ein Befehl (`docker-compose up`) startet das gesamte System.
- Automatisches Netzwerk: Container finden sich über den Service-Namen (Hostname).
